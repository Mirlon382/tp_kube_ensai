# EX1
Après avoir apply le deployment.yaml, on map notre app à une ip du cluster grâce à 
Pour vérifier que cela fonctionne, on va rentrer dans un des 3 pods puis curl le localhost avec le port 8080. Le swagger (endpoint /doc) apparaît bien avec la requete curl localhost:8080/docs

# EX2
On récupère le fichier ingress du cours. On modifie l'url et et le nom du service vers lequel il pointe.

# EX3
Ca marche :)
